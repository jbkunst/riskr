---
output:
  md_document:
    variant: markdown_github
---

<!-- README.md is generated from README.Rmd. Please edit that file -->

```{r, echo = FALSE}
knitr::opts_chunk$set(collapse = TRUE, comment = "#>", fig.path = "README-", message = FALSE)
```

# riskr

riskr is the next ite

You can install the latest development version from github with

    ```R
    devtools::install_github("jbkunst/riskr")
    ```
## Some functions

### Odds Table

```{r, message = FALSE, results='asis'}
library("riskr")

data("predictions")

score <- round(predictions$score * 1000)
target <- predictions$target
```

```{r, echo=TRUE, results='hide'}
knitr::kable(odds_table(score, target, nclass = 4))
```

```{r, echo=FALSE, results='asis'}
knitr::kable(odds_table(score, target, nclass = 4))
```